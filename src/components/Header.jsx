const Header = ({ ref }) => {
    return (
        <header ref={ref}>
            <h1>
                Mike's movie picks<span></span>
            </h1>
        </header>
    );
};

export default Header;
